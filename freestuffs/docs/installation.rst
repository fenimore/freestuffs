============
Installation
============

At the command line::

    pip install freestuffs

Or, if you have virtualenvwrapper installed:

.. code-block:: bash

    mkvirtualenv freestuffs
    pip install freestuffs
